#create database emp2;
#use emp2;
#create table employees(emp_id int(10) primary key,first_name varchar(30),last_name varchar(30),department varchar(30),salary double(10,2));
#CREATE TABLE employee_log(log_id int auto_increment primary key,emp_id int,action varchar(30),long_time timestamp default current_timestamp);

delimiter //

create trigger after_emp8
after insert on employees
for each row
begin
	insert into employee_log values(new.emp_id,'new employees added');
end  //
delimiter ;

INSERT INTO employees (emp_id, first_name, last_name, department, salary) VALUES (107, 'yash', 'parmar', 'IT', 59000);

select * from employee_log;

