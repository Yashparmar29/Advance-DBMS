delimiter //
create trigger before_per_show
before update on product
for each row
begin
	if new.qty < 100 then
		set new.qty = 100;
	end if;
end // 
delimiter ;

update product set  qty = 79 where id = 102;
select * from product;
